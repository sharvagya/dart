void main(List<String> args) {
  var age = 20;
  print(age = 30); //compound assignment
  print(age ~/=
      2); //if = comes after an operator it assigns the result back to the variable
  print(age *= 2);
  print(age -= 4);
}
